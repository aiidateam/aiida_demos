jupyter nbextension     enable --py --sys-prefix appmode
jupyter serverextension enable --py --sys-prefix appmode
reentry scan
export AIIDA_HOME=$HOME
export SCRIPTS=${HOME}/binder_scripts
${SCRIPTS}/start-singleuser.sh
